# Changesets 자동화 테스트 시나리오

이 문서는 Git Flow + Changesets 자동화 시스템을 검증하기 위한 테스트 시나리오를 정의합니다.

## 목차

- [사용자 제안 시나리오](#사용자-제안-시나리오)
- [추가 테스트 시나리오](#추가-테스트-시나리오)
- [테스트 우선순위](#테스트-우선순위)
- [테스트 체크리스트](#테스트-체크리스트)

---

## 사용자 제안 시나리오

### 시나리오 1: 신규 패키지 생성 및 애플리케이션 적용

**목적**: 새로운 패키지가 모노레포에 추가되고 기존 앱에서 사용될 때 자동화 검증

**단계**:
1. `packages/utils` 패키지 생성 (TypeScript 유틸리티)
2. `apps/web`에서 새 패키지 의존성 추가
3. Feature 브랜치에서 개발 후 develop 머지
4. Changeset이 두 패키지 모두 감지하는지 확인
5. Release 시 버전 관리 확인

**예상 결과**:
```
@repo/utils: 0.1.0 (신규, minor - 첫 릴리즈)
web: 0.0.4 → 0.0.5 (patch - 의존성 추가)
```

**검증 포인트**:
- [ ] Changeset이 `@repo/utils` 와 `web` 모두 감지
- [ ] 신규 패키지가 0.1.0으로 시작
- [ ] 의존성 추가가 patch 업데이트로 처리
- [ ] CHANGELOG가 양쪽 패키지에 생성

---

### 시나리오 2: 기존 패키지의 복수 버전 업데이트

**목적**: 여러 패키지가 동시에 업데이트될 때 일괄 처리 검증

**단계**:
1. `@repo/hooks`에 새 기능 추가 (`feat:`)
2. `@repo/ui`에 새 컴포넌트 추가 (`feat:`)
3. `apps/web`이 두 패키지 모두 사용하도록 수정
4. 단일 Feature 브랜치에서 모두 수정
5. Changeset이 3개 패키지 모두 감지
6. Release 시 의존성 체인 자동 업데이트 확인

**예상 결과**:
```
@repo/hooks: 0.3.0 → 0.4.0 (minor - 새 기능)
@repo/ui: 0.1.0 → 0.2.0 (minor - 새 컴포넌트)
web: 0.0.4 → 0.0.5 (patch - 의존성 업데이트)
```

**검증 포인트**:
- [ ] 단일 changeset 파일에 3개 패키지 모두 포함
- [ ] 각 패키지가 올바른 version bump type
- [ ] 의존성 업데이트가 자동으로 patch로 처리
- [ ] CHANGELOG에 의존성 변경 이력 기록

---

### 시나리오 3: 실제 릴리즈 프로세스 (복수 Feature Merge)

**목적**: 여러 Feature가 develop에 누적된 후 한 번에 릴리즈

**단계**:
1. **Feature A**: `@repo/hooks`에 기능 추가 (`feat:`) → develop 머지
   - Changeset A 생성됨
2. **Feature B**: `@repo/ui` 버그 수정 (`fix:`) → develop 머지
   - Changeset B 생성됨
3. **Feature C**: `apps/web` 개선 (`fix:`) → develop 머지
   - Changeset C 생성됨
4. Develop에 3개의 changeset 누적 확인
5. `release/vX.X.X` 브랜치 생성 & push
6. 3개의 changeset이 하나의 버전 업데이트로 통합
7. CHANGELOG에 모든 변경사항 포함 확인
8. Auto-merge로 자동 배포

**예상 결과**:
```
Develop에 changeset 3개 누적:
- auto-123.md (hooks minor)
- auto-456.md (ui patch)
- auto-789.md (web patch)

Release 시 한 번에 버전 업데이트:
@repo/hooks: 0.4.0 → 0.5.0 (minor)
@repo/ui: 0.2.0 → 0.2.1 (patch)
web: 0.0.5 → 0.0.6 (patch)
```

**검증 포인트**:
- [ ] 각 Feature 머지 시 독립적인 changeset 생성
- [ ] Develop에 여러 changeset 파일 누적
- [ ] Release 브랜치 생성 시 모든 changeset 소비
- [ ] 하나의 "chore(release): version packages" 커밋으로 통합
- [ ] CHANGELOG에 모든 변경사항이 시간순으로 정렬
- [ ] PR이 자동 생성되고 auto-merge 활성화
- [ ] Github Release 태그 자동 생성

---

## 추가 테스트 시나리오

### 시나리오 4: Hotfix (긴급 수정)

**목적**: Production 긴급 수정 프로세스 검증

**Git Flow Hotfix 전략**:
```
main → hotfix/critical-bug → main
                           → develop (백포트)
```

**단계**:
1. Main에서 `hotfix/critical-bug` 브랜치 생성
2. 버그 수정 (`fix(hooks): critical security issue`)
3. Main으로 직접 PR 생성
4. 수동으로 changeset 생성하거나 자동화 확인
5. Main 머지 후 즉시 release 태그 생성
6. Hotfix를 develop으로 백포트

**예상 결과**:
```
@repo/hooks: 1.0.0 → 1.0.1 (patch - hotfix)
```

**검증 포인트**:
- [ ] Hotfix 브랜치가 main에서 생성
- [ ] Changeset 처리 방식 확인 (수동 or 자동)
- [ ] Main 머지 후 즉시 배포
- [ ] Develop 백포트 시 충돌 없음

**참고**: 현재 자동화는 develop 기반이므로, hotfix는 수동 changeset 필요할 수 있음

---

### 시나리오 5: Breaking Change (Major Version)

**목적**: Major 버전 업데이트 자동 감지 검증

**단계**:
1. `@repo/hooks`의 API 변경 (breaking change)
2. Commit 메시지에 `feat!:` 사용
   ```bash
   git commit -m "feat(hooks)!: change useCounter return type"
   ```
3. 또는 Breaking Change 명시:
   ```bash
   git commit -m "feat(hooks): change API

   BREAKING CHANGE: useCounter now returns object instead of array"
   ```
4. Changeset이 major 버전으로 감지하는지 확인
5. 의존하는 앱들이 적절히 업데이트되는지 확인

**예상 결과**:
```
Commit: "feat(hooks)!: change useCounter API"
→ @repo/hooks: 0.5.0 → 1.0.0 (major)
→ web: 0.0.6 → 0.0.7 (patch - 의존성 major 변경)
```

**검증 포인트**:
- [ ] `feat!:` 또는 `BREAKING CHANGE:` 감지
- [ ] Changeset이 major로 생성
- [ ] CHANGELOG에 Breaking Change 섹션 생성
- [ ] 의존하는 패키지가 자동으로 업데이트

---

### 시나리오 6: 의존성 체인 업데이트

**목적**: A → B → C 의존성 체인에서 자동 전파 검증

**의존성 구조**:
```
@repo/typescript-config (base)
  ↓ (devDependency)
@repo/hooks
  ↓ (dependency)
web
```

**단계**:
1. `@repo/typescript-config` 수정
   - 현재 changesets ignore에서 제외 필요
2. `.changeset/config.json`에서 ignore 제거
3. Feature 브랜치로 변경
4. `@repo/hooks` 빌드 필요 확인
5. `web` 빌드 필요 확인
6. Release 시 3단계 의존성 모두 업데이트

**예상 결과**:
```
@repo/typescript-config: 1.0.0 → 1.1.0 (minor)
@repo/hooks: 1.0.0 → 1.0.1 (patch - devDep 업데이트)
web: 0.0.7 → 0.0.8 (patch - 간접 의존성)
```

**검증 포인트**:
- [ ] Config 패키지 변경이 감지됨
- [ ] DevDependency 업데이트가 patch로 처리
- [ ] 간접 의존성도 업데이트됨

---

### 시나리오 7: 동시 작업 (Concurrent Features)

**목적**: 여러 개발자가 동시에 작업할 때 충돌 없이 처리되는지 검증

**단계**:
1. 개발자 A: `feature/user-auth` 시작
2. 개발자 B: `feature/dashboard` 시작 (동시)
3. 개발자 A: develop PR 생성 (#10)
4. 개발자 B: develop PR 생성 (#11)
5. PR #10 먼저 머지 → `auto-1234.md` 생성
6. PR #11 머지 → `auto-1235.md` 생성
7. Release 시 두 changeset 모두 처리

**예상 결과**:
```
Develop에 두 개의 독립적인 changeset:
- auto-1234.md (hooks minor, web patch)
- auto-1235.md (ui minor, web patch)

Release 시:
@repo/hooks: 1.0.0 → 1.1.0
@repo/ui: 0.2.1 → 0.3.0
web: 0.0.7 → 0.0.8
```

**검증 포인트**:
- [ ] 각 PR이 독립적인 changeset 생성
- [ ] Changeset 파일명 충돌 없음 (timestamp 기반)
- [ ] Release 시 두 changeset 모두 소비
- [ ] 동일 패키지의 여러 변경사항이 올바르게 병합

---

### 시나리오 8: Release 브랜치에서 수정

**목적**: Release 브랜치에서 발견된 버그 수정 프로세스

**단계**:
1. `release/v1.0.0` 브랜치 생성 → 자동 버전 업데이트
2. QA 테스트 중 버그 발견
3. Release 브랜치에서 직접 수정
   ```bash
   git checkout release/v1.0.0
   git commit -m "fix(ui): fix button alignment"
   git push
   ```
4. Workflow가 다시 실행되는지 확인
5. 버전이 재계산되는지 확인
6. Main 머지 후 develop으로 백포트

**예상 결과**:
```
초기: @repo/ui 0.3.0 → 0.4.0
버그 수정 후: @repo/ui 0.3.0 → 0.4.1 (patch 추가)
```

**검증 포인트**:
- [ ] Release 브랜치에서 추가 커밋 허용
- [ ] 기존 버전 업데이트가 덮어쓰기되지 않음
- [ ] CHANGELOG에 release 브랜치 수정사항 포함
- [ ] Develop 백포트 시 충돌 해결

---

### 시나리오 9: 빈 Changeset (No Changes)

**목적**: Changeset 없이 Release 시도 시 동작 검증

**단계**:
1. Develop에 changeset 없는 상태 (모든 changeset 소비됨)
2. `release/v1.1.0` 브랜치 생성 & push
3. `changeset version` 실행 결과 확인
4. 워크플로우 동작 확인

**예상 결과**:
```
No changesets found, skipping version update
또는
"No changes to commit" 메시지
```

**검증 포인트**:
- [ ] 에러 없이 정상 완료
- [ ] PR이 생성되지 않음
- [ ] 또는 "No changes" PR 생성 후 자동 닫힘

---

### 시나리오 10: Changeset 충돌

**목적**: 동일 패키지를 여러 Feature가 동시에 수정할 때 처리

**단계**:
1. Feature A: `@repo/hooks` v0.5.0 → v0.6.0 (minor)
2. Feature B: `@repo/hooks` v0.5.0 → v0.6.0 (minor) - 동시 개발
3. Feature A develop 머지 → changeset A
4. Feature B develop 머지 → changeset B
5. Release 시 두 changeset 통합

**예상 결과**:
```
Changeset A: @repo/hooks minor
Changeset B: @repo/hooks minor
→ Release: @repo/hooks 0.5.0 → 0.6.0 (minor 하나만 적용)
```

**검증 포인트**:
- [ ] 두 changeset이 충돌 없이 생성
- [ ] Release 시 올바른 버전 계산 (minor + minor = minor)
- [ ] CHANGELOG에 두 변경사항 모두 기록

---

### 시나리오 11: 수동 Changeset 추가

**목적**: 수동으로 추가된 changeset과 자동 changeset 혼합

**단계**:
1. Feature 개발 중 개발자가 수동으로 changeset 추가
   ```bash
   pnpm changeset
   ```
2. 같은 PR에서 자동 changeset도 트리거됨
3. 두 changeset 파일이 공존하는지 확인
4. Release 시 중복 처리 없이 통합

**예상 결과**:
```
.changeset/
  manual-beautiful-cats.md (수동)
  auto-1234567890.md (자동)

→ Release 시 둘 다 처리됨
```

**검증 포인트**:
- [ ] 수동 changeset 감지하여 자동 생성 스킵
- [ ] 또는 두 changeset 모두 유지
- [ ] Release 시 중복 버전 업데이트 없음

---

### 시나리오 12: Config 패키지 변경

**목적**: eslint-config, typescript-config 변경 시 영향도 확인

**단계**:
1. `@repo/eslint-config` 규칙 변경
2. 모든 의존 패키지 및 앱에서 lint 재실행
3. Config 패키지는 버전 관리 안 되는지 확인 (ignore 설정)

**현재 설정**:
```json
// .changeset/config.json
"ignore": []
```

현재는 ignore 목록이 비어있으므로 config 패키지도 버전 관리됨.

**검증 포인트**:
- [ ] Config 패키지 변경 시 changeset 생성 여부 결정
- [ ] 의존하는 패키지들의 자동 업데이트 확인
- [ ] DevDependency로 처리되는지 확인

---

## 테스트 우선순위

### P0 (필수 - 핵심 기능)
1. ✅ **시나리오 3**: 실제 릴리즈 프로세스 (복수 Feature)
2. ✅ **시나리오 5**: Breaking Change (Major 버전)
3. ✅ **시나리오 7**: 동시 작업 (Concurrent Features)

### P1 (중요 - 실전 시나리오)
4. ⚠️ **시나리오 1**: 신규 패키지 생성
5. ⚠️ **시나리오 2**: 복수 버전 업데이트
6. ⚠️ **시나리오 8**: Release 브랜치 수정

### P2 (권장 - 엣지 케이스)
7. 📝 **시나리오 4**: Hotfix
8. 📝 **시나리오 6**: 의존성 체인
9. 📝 **시나리오 9**: 빈 Changeset
10. 📝 **시나리오 10**: Changeset 충돌
11. 📝 **시나리오 11**: 수동 Changeset
12. 📝 **시나리오 12**: Config 패키지

---

## 테스트 체크리스트

각 시나리오 테스트 후 다음 항목을 확인하세요:

### 자동화 검증
- [ ] Changeset이 자동으로 생성되었는가?
- [ ] 올바른 version bump type이 감지되었는가? (major/minor/patch)
- [ ] Release PR이 자동으로 생성되었는가?
- [ ] Auto-merge가 활성화되었는가?
- [ ] Github Release 태그가 생성되었는가?

### 버전 관리
- [ ] package.json의 버전이 올바르게 업데이트되었는가?
- [ ] CHANGELOG.md가 자동으로 생성/업데이트되었는가?
- [ ] 의존성 체인이 올바르게 업데이트되었는가?
- [ ] 중복 버전 업데이트가 없는가?

### Git Flow
- [ ] Feature → Develop 플로우가 정상 작동하는가?
- [ ] Release 브랜치가 올바르게 처리되는가?
- [ ] Main 브랜치가 항상 stable한 상태인가?
- [ ] Develop 백포트가 필요한 경우 정상적으로 처리되는가?

### 문서화
- [ ] CHANGELOG가 읽기 쉽게 작성되었는가?
- [ ] PR 설명이 명확한가?
- [ ] Release notes가 유용한가?

---

## 테스트 실행 방법

### 1. 환경 준비
```bash
cd /path/to/changesets-test-repo
git checkout develop
git pull origin develop
```

### 2. Feature 브랜치 생성
```bash
git checkout -b feature/test-scenario-X
# 코드 수정
git commit -m "feat(package): description"
```

### 3. PR 생성 및 머지
```bash
gh pr create --base develop --title "..." --body "..."
gh pr merge <PR_NUMBER> --squash
```

### 4. Changeset 확인
```bash
ls -la .changeset/*.md
cat .changeset/auto-*.md
```

### 5. Release 생성
```bash
git checkout develop
git pull origin develop
git checkout -b release/vX.X.X
git push origin release/vX.X.X
```

### 6. 결과 확인
```bash
# PR 확인
gh pr list --head release/vX.X.X

# 워크플로우 확인
gh run list --branch release/vX.X.X

# Release 확인
gh release list
```

---

## 테스트 결과 기록

각 시나리오 테스트 후 결과를 기록하세요:

```markdown
## 시나리오 X: [제목]

**테스트 날짜**: YYYY-MM-DD
**테스터**: [이름]
**결과**: ✅ 성공 / ⚠️ 부분 성공 / ❌ 실패

**상세 내용**:
- Changeset: [파일명]
- PR: #[번호]
- Release: [태그명]

**이슈**:
- [발견된 문제점]

**개선사항**:
- [제안사항]
```

---

## 참고 문서

- [Automation Guide](./AUTOMATION_GUIDE.md) - 전체 자동화 시스템 가이드
- [Workflows](./WORKFLOWS.md) - 워크플로우 상세 설명
- [Developer Guide](./DEVELOPER_GUIDE.md) - 개발자 실용 가이드
- [Branching Strategy](../BRANCHING_STRATEGY.md) - Git Flow 전략
