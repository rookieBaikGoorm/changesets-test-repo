name: Hotfix Automation

on:
  push:
    branches:
      - main

concurrency: ${{ github.workflow }}-${{ github.ref }}

jobs:
  hotfix-release:
    name: Hotfix Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Check if hotfix branch merge
        id: check-hotfix
        run: |
          COMMIT_MSG=$(git log -1 --pretty=%B)

          if echo "$COMMIT_MSG" | grep -qE "Merge branch 'hotfix/"; then
            echo "is_hotfix=true" >> $GITHUB_OUTPUT
            echo "🚨 Hotfix 브랜치 병합 감지됨"

            HOTFIX_BRANCH=$(echo "$COMMIT_MSG" | grep -oE "hotfix/[^ ']*" | head -1)
            echo "hotfix_branch=$HOTFIX_BRANCH" >> $GITHUB_OUTPUT
            echo "Hotfix 브랜치: $HOTFIX_BRANCH"
          else
            echo "is_hotfix=false" >> $GITHUB_OUTPUT
            echo "ℹ️ Hotfix 병합이 아님, 스킵"
          fi

      - name: Create tags and GitHub Releases
        if: steps.check-hotfix.outputs.is_hotfix == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "📦 Hotfix 태그 생성 중..."
          echo ""
          echo "ℹ️  버전 업데이트는 git-flow hook에서 이미 완료되었습니다"
          echo ""

          # 모든 package.json 파일을 찾아서 태그 생성
          for pkg_json in packages/*/package.json apps/*/package.json; do
            if [ ! -f "$pkg_json" ]; then continue; fi

            PKG_NAME=$(node -p "require('./$pkg_json').name" 2>/dev/null || echo "")
            if [ -z "$PKG_NAME" ]; then continue; fi

            PKG_VERSION=$(node -p "require('./$pkg_json').version" 2>/dev/null || echo "")
            if [ -z "$PKG_VERSION" ]; then continue; fi

            TAG_NAME="${PKG_NAME}@${PKG_VERSION}"

            # 태그가 이미 존재하는지 확인
            if ! git rev-parse "$TAG_NAME" >/dev/null 2>&1; then
              echo "📦 Hotfix 태그 생성: $TAG_NAME"
              git tag "$TAG_NAME"
              git push origin "$TAG_NAME"

              # Github Release 생성
              CHANGELOG_PATH="${pkg_json%package.json}CHANGELOG.md"
              if [ -f "$CHANGELOG_PATH" ]; then
                RELEASE_NOTES=$(awk "/## $PKG_VERSION/,/## [0-9]/" "$CHANGELOG_PATH" | sed '1d;$d')

                if [ -n "$RELEASE_NOTES" ]; then
                  gh release create "$TAG_NAME" \
                    --title "$TAG_NAME (Hotfix)" \
                    --notes "$RELEASE_NOTES" || echo "⚠️ Release 생성 실패: $TAG_NAME"
                else
                  gh release create "$TAG_NAME" \
                    --title "$TAG_NAME (Hotfix)" \
                    --notes "Hotfix Release $TAG_NAME" || echo "⚠️ Release 생성 실패: $TAG_NAME"
                fi
              else
                gh release create "$TAG_NAME" \
                  --title "$TAG_NAME (Hotfix)" \
                  --notes "Hotfix Release $TAG_NAME" || echo "⚠️ Release 생성 실패: $TAG_NAME"
              fi

              echo "✅ Hotfix 태그 및 릴리즈 생성 완료: $TAG_NAME"
              echo ""
            else
              echo "✅ 태그 $TAG_NAME 이미 존재함, 스킵"
              echo ""
            fi
          done

          echo "✅ Hotfix 릴리즈 배포 완료"
